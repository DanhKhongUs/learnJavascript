/* 
    TODOS -> Code thuần (Là dùng kiến thức đã học áp dụng thẳng vào luôn)

    // Dùng kiến thức code thuần để xây dựng lybrary 
    // Từ library xây dựng ra các ứng dụng
    Library -> TODOS -> Cách nghĩ so với code thuần khác hẳn 180deg

*/

import { attach } from './store.js'
import App from './component/App.js'

attach(App, document.getElementById('root'))

